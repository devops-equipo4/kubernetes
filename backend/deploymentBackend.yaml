apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    name: grupo-4
    grupo: grupo-4
    product: diplomado
    tierApp: Backend
    app: devopsg4-app
    tier: backend
    type: service
  name: devopsg4-app
  namespace: grupo4
spec:
  selector:
    matchLabels:
      app:
        devopsg4-app
  replicas: 2
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate  
  template:
    metadata:
      labels:
        app: devopsg4-app
    spec:
      containers:
      - name: devopsg4-app
        image: luismellareyes/devopsg4
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
   name: devopsg4-app-service
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8084
      protocol: TCP
  selector:
    app: devopsg4-app
    tier: backend
  type: LoadBalancer
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: devopsg4-app-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: devopsg4-app
  minReplicas: 2
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devopsg4-app-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
    - host: localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: devopsg4-app-service
                port:
                  number: 8084

